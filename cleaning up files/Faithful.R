x=c(2,1,2,8,2)
(2+1+2+8+2)/5
mean(x)


sort(faithful.eruptions)
n <- length(faithful$waiting)

sorted_waiting <- sort(faithful$waiting)
sorted_waiting[n/2]
median(faithful$waiting)
range(faithful$waiting)
max(faithful$waiting)-min(faithful$waiting)
IQR(faithful$waiting)